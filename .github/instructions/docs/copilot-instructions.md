Documentation files should be written in Markdown and normally use a Markdown-related extension (for example, `.md` or `.markdown`).

Documentation file names should follow a clear and consistent convention such as CamelCase (`MyDoc.md`), kebab-case (`my-doc.md`), or snake_case (`my_doc.md`); choose the style that matches the surrounding docs in this repository.